# Changelog

## 0.3.0

- 新增 Sync v2 树同步：服务端支持时自动选择 Sync v2，否则回退 Legacy v1；同步中心显示当前协议（`Sync v2` / `Legacy v1`），不提供手动协议选择器。
- v2 支持文件夹映射与空文件夹：嵌套目录、空目录和页面可跨设备双向同步，文件夹 ID 与路径保持一致；Legacy v1 保留原页面语义。
- 补齐 v2 端到端与资源上限回归：混合目录/页面跨桌面与移动端往返、空文件夹保留，以及 maxClientSpaceFolders / maxSnapshotObjects / maxDeltaItems / maxResponseBytes / maxDocumentTreeBytes / maxPushChanges 边界校验。
- 当服务器的 V2 Space 列表暂时返回 5xx 时，自动使用 V1 列表补齐 Space 名称、角色与发布权限；实际 capabilities、head、delta、snapshot 与 push 继续使用 V2，鉴权错误不会被兼容回退掩盖。

## 0.2.12

- 自动合并现在能识别双方在文末追加内容、且 `diff3` 已生成完整包含双方的安全候选，不再误报为必须人工处理的结构化冲突。
- 冲突或首次绑定尚未选择时，“确认执行”保持禁用并显示待处理项数，避免点击后才报“存在未解决的结构化冲突”。

## 0.2.11

- 修复同步中心在预览窗口打开后才关闭父窗口，导致冲突弹窗可见但鼠标和键盘事件穿透到背景的问题。
- 冲突预览改为直接显示安全截断的 base/local/remote 摘要，不再卡在“正在加载预览…”；完整冲突内容仍保存在暂存文件中供确认执行使用。
- 拉取完成后进入推送预览也统一采用“先关闭当前窗口，再打开下一窗口”的安全切换顺序。

## 0.2.10

- 修复 Windows 桌面端“处理冲突与绑定”弹窗的横向挤压和巨大空白；说明与表单控件改为容器响应式排列。
- 不再显示已自动决定的绑定项，真正待处理的冲突直接可见；取消和确认操作区始终可达，避免界面看似卡死。

## 0.2.9

- 修复 Windows 和窄桌面窗口中同步策略说明被三个按钮挤成窄列的问题；说明现在独占整行，按钮依据弹窗宽度自动横排、换行或纵向排列。

## 0.2.8

- Space → Vault 映射改为持久保存在库级 `data.json`，可从 0.2.7 本地 envelope 一次性迁移；重启、禁用/启用和插件更新都不再丢失同步目录。
- `data.json` 只保存非敏感的服务器 URL 和映射；设备、凭据、Vault 与服务器实例身份仍留在 Obsidian 本地安全存储。
- 映射根目录缺失或变成文件时明确阻断同步并保留映射，设置页显示重建文件夹或更改映射的操作指引。
- 兼容 AgentWiki 可读标题路径：服务端将 `p-<hash>.md` 改为 `标题.md` / `标题 (2).md` 时，插件只重命名文件；本地与远端双向改名显式进入路径冲突。
- 路径迁移和标题改名不改写文章正文，包括与标题同名的 Markdown H1。

## 0.2.7

- 同步中心新增明确 Space 选择，pending 映射和只读 Space 也可正确 Pull；重复 Space 映射现在会被拒绝。
- 文件打开事件不再自动访问服务器；仅显式连接、打开/刷新同步中心和确认同步会联网。
- “使用本地内容”现在会先展示 Pull 最终动作并取得确认，不会在 Push 预览前提前改写 Vault。
- 扫描、下载、合并和分批上传增加进度、事件循环让步与安全取消；finalize/Pull 事务原子阶段不可中断。
- 修复 Obsidian Community 自动审核的设置页标题错误，并将用户文件移除统一交给 FileManager 以遵循回收站偏好。
- 接入 Obsidian 官方 ESLint 审核规则，锁定运行时依赖并提交 lockfile，保证市场构建可复现。
- 新增 GitHub Actions 自动构建、完整验证、Release 发布和 release asset provenance attestation。

## 0.2.6

- 修复基线 generation 无限累积的存储泄漏：每次同步都会写入完整快照副本且从不清理，现在提交后自动清理不再被指针、回退候选或事务日志引用的旧 generation。
- 修复设置页移动端响应式样式未生效的问题（class 钩子在 0.2.4 发布时意外缺失）。
- 空间列表加载失败不再误报为“暂无可访问的空间”，现在显示具体错误并提供重试按钮。
- 同步确认弹窗失败时显示友好中文错误，而不是原始英文错误码。

## 0.2.5

- 修复归档冲突选择“手动”时本地文件被误删、手动内容丢失的数据丢失缺陷：现在保留文件并写入手动内容。
- 修复服务器地址大小写或默认端口与规范化形式不一致时，连接后同步报“连接身份不匹配”、重启后插件无法加载的问题。
- 拉取事务内部临时重命名不再污染移动提示（move hints），消除无谓的文件读写。

## 0.2.4

- 优化手机端显示布局：控件自动换行，输入框、下拉框占满行宽，长路径与哈希正常折行。
- 手机端同步中心三个策略按钮改为竖排全宽，不再被屏幕截断。
- 修复设置页“添加映射”一行在窄屏上溢出的问题；桌面端布局保持不变。

## 0.2.3

- 新装插件默认预填官方服务器地址，手机端无需手动输入完整 URL。
- 连接失败后重试不再被旧的待处理连接日志永久卡死：身份不匹配的陈旧日志会自动丢弃并重新开始。
- 修复连接失败被误报为“连接成功”的错误提示。

## 0.2.2

- 修复哈希文件名双前缀缺陷：opaqueFileKey 已含 p- 前缀，外层不再重复拼接。
- verify 增加旧版平铺哈希路径回退，未迁移的旧基线也能正常读取。
- 修复后启动迁移可正确将旧哈希文件改名为可读路径。

## 0.2.1

- 修复 manifest 描述以符合社区目录要求。

## 0.2.0

- 新增同步中心：点击同步按钮直接显示本地与服务器的逐文件差异日志。
- 支持三种同步策略：自动合并（推荐）、使用本地内容、使用服务器内容。
- 状态栏可点击打开同步中心；命令面板新增“打开同步中心”。
- 移除旧的 Status/Pull/Push 三命令模式。

## 0.1.0

- 初始化 AgentWiki Sync 插件、公开协议兼容边界和 Obsidian 原生 UI。
- 实现严格路径/正文校验、Status、三方合并、首次绑定和分页客户端。
- 实现 crash-safe generation、Pull 条件事务和 Push session 恢复。
- 增加 fake AgentWiki 多设备流程、故障注入和发布安全门禁。
