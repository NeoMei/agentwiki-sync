# 0.4.0 publication and official asset verification

2026-09-08. GitHub publication, existing-device official asset checks and fresh empty Vault first-load/reload checks PASS. The last restricted-mode gate was closed after the user's explicit permission; the historical blocked attempt is retained below. Obsidian staff manual review remains an external status, not a completed project acceptance check.

## Integration and build

Reviewed release commit `cfd09cf7b5cf4c027a3d44707a224f04ac39ac89` fast-forwarded into local main, preserving all untracked files in both checkouts. Feature worktree remains intact. Final independent audit closed I1/I2/M1 with remaining C/I/M=0/0/0. `docs/releases/0.4.0.md` was explicitly added despite the generic releases/ ignore pattern.

Fresh feature check: 66 files / 1313 tests PASS, all formatting/types/build/bundle/release checks PASS, 17 existing lint warnings and 0 errors. npm audit: 0 vulnerabilities. Log `/tmp/agentwiki-native-final-277.7NXHQ5/release-final-check.log`, SHA256 `034cf897f932dfb3d298a9e483c1e5f7ef234c46abf28c3b0f2a6ff003a5d323`.

Main checkout check initially failed because eslint . discovered a pre-existing untracked `agentwiki/apps/server/src/integrations/obsidian/sync-v3-immutable-revision.service.ts` outside the plugin tsconfig. That file was not changed, deleted or added to the release. Exported the exact merged commit using git archive into `/tmp/agentwiki-040-merged-clean.99Wdu8`, then npm ci and the unmodified full npm run check passed: 66 files / 1313 tests, matching three asset hashes. Clean merged log SHA256 `cab608160f007416b311fe274fb90a0b8baac221fbfc1e752670ebad614e1e7e`. Original failed log retained as `merged-check.log` in the evidence directory.

## GitHub: PASS

Rechecked remote main=`de1582cc541b32a308554c178a3ce385827f8054`, latest Release0.3.0 and no 0.4.0 tag, then atomically pushed main plus new annotated numeric tag0.4.0. No force-push or old tag replacement.

- Tag peeled commit: `cfd09cf7b5cf4c027a3d44707a224f04ac39ac89`.
- [Release workflow](https://github.com/NeoMei/agentwiki-sync/actions/runs/34239717977): completed/success, exact commit, full CI checks and attestations passed.
- [GitHub Release 0.4.0](https://github.com/NeoMei/agentwiki-sync/releases/tag/0.4.0): published `2026-09-08T14:41:20Z` (22:41:20 Asia/Shanghai), draft=false, prerelease=false; three uploaded assets.
- Downloaded all three official assets to `/tmp/agentwiki-official-040.Icfh9i`. `gh attestation verify --repo NeoMei/agentwiki-sync` succeeded separately for main.js, manifest.json and styles.css; JSON proofs retained in the native evidence directory.

| Official asset | Bytes | SHA256 |
| --- | --- | --- |
| main.js | 1713839 | `277116a922367f65c34ec4bc235308876c8003e1d4da5918250c890b410b3c27` |
| manifest.json | 232 | `7b001849eafc37311a720c56d62f37704b1697ea75a4a5b801423d4be443daee` |
| styles.css | 4880 | `5dfe15839220724a57a64ad7ab136761ba2ffaaff3c0235faac1cf1238e762b8` |

These match the frozen native/public-tested candidate exactly, both the API asset digests and independently downloaded bytes.

## Existing desktop and Android official installation: PASS

Backed up only each client's three plugin assets under the official download directory, then installed the three downloaded files. No data.json or credentials copied. Reloaded the actual plugin, verified loaded=true/version0.4.0 and all three installed hashes, and ran actual local-preference sync through the public service:

- Desktop acceptance Vault, five mappings: test Space `cmtsqgn5801qc1368308qri6t` remains sequence5 / revision `cmtsrm7xt01xi1368glcazhlw`.
- Android NeoMei-Docs, three mappings: test Space `cmtsqgnlg01qf13688ya8k7h6` remains sequence4 / revision `cmtsrj7mo01w71368hhhnohbd`.

Both actual syncs returned no-op, only two GET head requests. Body/image/mappings unchanged; temporary transport hook restored in finally. Evidence:

- `desktop-official-install.json` SHA256 `466a4ab3e80d2b2f32b3d9b2477fbfdf6b0ec0b66d8c7cde015af8646d5a9940`.
- `android-official-install.json` SHA256 `acd1a729f907bd75c7af9fbb1abcc94d65ab15c311aea4567be49c4986dd5b7d`.

## Fresh Vault: installation PASS; initial first load blocked by restricted mode (subsequently resolved)

Created new empty `AgentWiki-Release-040-20260908` through the real Obsidian Vault UI, under `/Users/neomei/Obsidian/`. No existing Vault content copied. Actual Obsidian community installation command `plugin:install id=agentwiki-sync enable` downloaded 0.4.0 and reported installed/enabled; plugin info and enabled list also reported 0.4.0.

The community installer appends exactly `\n/* nosourcemap */` (18 bytes) to main.js: installed length1713857, SHA256 `2f23bbb25b322f2a9d739bf89ad8a6b2bee5f748ce83340bce493fe6137b7e1c`. A byte comparison proved its entire 1713839-byte prefix equals the official asset, with no other differences. Manifest/styles hashes equal official assets.

However, the actual renderer returned `{vault:"AgentWiki-Release-040-20260908",loaded:false,enabled:true}`. The real settings UI explains why: this new Vault remains in restricted mode and presents “退出受限模式”, with one installed plugin available to restore. No product exception or loaded state is inferred from CLI metadata. Did not exit restricted mode or bypass it programmatically; awaiting specific confirmation for this security-mode change. No account connection or mappings were added to the new Vault. This is the remaining first-load gate, not a failure of the already verified existing-device sync flows.

## Independent release channels

- Obsidian community directory currently contains `agentwiki-sync` pointing to `neomei/agentwiki-sync`; actual fresh install retrieves0.4.0. Directory description explicitly says it has not been manually reviewed by Obsidian staff. Manual review is not claimed complete.
- Fresh npm registry lookup: `@neomei/agentwiki-local-sync` version/latest=`0.9.1`. This plugin release did not publish npm packages.
- Fresh production `/api/health`: status/database/redis/auditPersistence/attachmentStorage all ok; both official-client real sync checks above succeeded. Read-only SSH failed `Permission denied (publickey,password)`, so this round does not claim a newly verified server checkout SHA. No server redeployment was needed or performed for the unchanged plugin candidate. Earlier deployed fixes and migration evidence remain in their chronological reports.

All fixtures, rollback evidence and user-owned untracked files retained. GitHub tag remains immutable; this post-release documentation is a later main commit. See [native gates](2026-09-08-final-native-release-gates.md) for the actual pre-publication matrix rather than treating a release or health response as sync proof.

## Final fresh-Vault gate closure: PASS

The user explicitly answered “允许” to exiting restricted mode only in `AgentWiki-Release-040-20260908`. Rechecked the actual settings window title and used the real “退出受限模式” button; the UI then displayed the installed AgentWiki Sync v0.4.0 and an option to turn restricted mode back on. Only this new empty Vault's mode changed. No authentication, connection, sync mapping or other Vault setting was modified.

Actual renderer verification, repeated after `plugin:reload id=agentwiki-sync`, returned `{vault:"AgentWiki-Release-040-20260908",version:"0.4.0",loaded:true,enabled:true,mappingCount:0}`. All installed hashes remain as recorded above; repeated byte comparison proves the main.js body equals the official asset with only the exact installer suffix. The assertion command exited0/PASS. Evidence `/tmp/agentwiki-native-final-277.7NXHQ5/fresh-vault-loaded.log`, SHA256 `3826821ef8a66449ab1fc8c7111d5b6067d6ac907afbafa6d81cfc27e868ba79`.

This closes fresh-install first-load acceptance, not a new authenticated sync test in the empty Vault; real official-package sync was already verified in the authorized desktop and Android fixtures. No product code or release asset changed, so earlier candidate test evidence remains applicable. Fresh GitHub lookup still reports0.4.0 as non-draft/non-prerelease. All scoped implementation, acceptance and GitHub release gates are now closed; Obsidian staff manual review is still not claimed complete.
