# Q3 同步修复候选验收

基线 `57514242287c763837d7e1472909eff4b88ded0d`，修复分支 `codex/q3-fixes-20260914`。用户授权修复；版本保持0.5.0，未发布、未安装日常Vault、未进行Windows原生验收。

## 修复行为

- 控制存储明确区分已知版本形状错误、哈希错误和未知/未来版本；先校验再写`.next`，避免失败写入污染恢复候选。未来版本继续拒绝，不清除故障控制文件。
- v3应用后校验传入正确总正文容量，保留完整检查，并输出对象/字段级差异，不输出正文。
- v2按服务器恢复支持服务器版本未变而本地有删除/修改的场景；自动合并的删除语义保持。恢复有明确预览确认。
- 恢复预览绑定本地原始路径状态及目录成员；确认前或事务准备时发生变化，拒绝继续并保留恢复证据。
- 要删除目录存在未托管后代（PDF、图片、隐藏文件等）时阻止恢复，避免递归删除未展示文件。
- 非终态恢复日志仍存在时，保留已暂存正文，不因关闭对话框丢失恢复材料。

- 普通V2目录事务在日志中保存枚举模式，使准备、执行和恢复对隐藏子目录处理一致；兼容旧日志无该字段，并保留严格字段/类型校验。新增静态隐藏目录移动与恢复、四项日志字段边界回归。

## 检查

`npm run check`通过：68文件、1385项测试；类型、格式、生产构建、包安全与版本元数据检查通过，lint 0错误/19警告。新增恢复边界回归先红后绿。独立审查结果在主项目Q3验证记录中收口。

## 人工验收步骤

1. 在隔离Vault配置两空间，拉取后重启插件，分别再次拉取；另一映射控制文件不应被污染。
2. v3产生本地/远端冲突，保留本地；核对正文、目录、附件及随后可提交的本地变更。
3. v2删除2目录5页面，选择按服务器恢复；确认恢复。另测自动合并，应保留可提交删除。
4. 打开恢复预览后再次编辑正文或添加目录后代，再确认：应拒绝旧预览且保留本地文件。
5. 待删除目录放未引用PDF、图片或隐藏文件：应提示阻断，未托管文件保持。
6. 新3/4原始Windows报错仍需实际安装版本与脱敏诊断定位；便携CRLF/中文路径用例不能替代原生现场。保留相关主/prev/next控制文件，不靠删除基线绕过检查。
