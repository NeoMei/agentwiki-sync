# AgentWiki Sync 0.5.2

- Repairs stale Space mappings after reconnecting a device with a new credential.
- Explains the recovery path when the current credential cannot access a mapped Space.

Validated with the full plugin check suite and a real Obsidian reconnect flow.
