# AgentWiki Sync 0.5.3

- Recovers confirmed page pushes after the server-side session expires or is no longer found by rebuilding the session from durable local staging data.
- Accepts the recoverable Sync v2 `finalizing` state and resumes the publish operation without re-uploading received batches.
- Corrects the Sync Center recovery copy so page pushes are shown as page synchronization rather than image upgrades.

Validated with the full plugin check suite and the real NeoMei-Docs Vault recovery flow.
